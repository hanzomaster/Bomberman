package entities;

import javafx.scene.image.Image;

public class Monsters extends Entity {
  public Monsters(int x, int y, Image image) {
    super(x, y, image);
  }

  @Override
  public void update() {}
}
